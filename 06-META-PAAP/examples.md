# meta-paap — Example invocations and outputs

> Sample inputs that produced each of the four evaluated skills, and what the output looked like.

**Status:** Skeleton — content lands in Phase 3, drawn from the four test cases in [`../05-EVALUATION/`](../05-EVALUATION/).

---

## Outline

For each of the four test cases:
- The original workflow description provided to `meta-paap`
- The five elicitation answers given
- Composition candidates `meta-paap` suggested
- The architecture spec it produced
- The final generated SKILL.md (or a link to it)
- One iteration's worth of fixes applied

### Examples to include

1. `/skill-audit` — analytical tool, parallel agents
2. `/article-forge` — knowledge processing pipeline, 3 composed skills
3. `/review-plan` — dual-mode plan reviewer
4. `/idea-to-pr` — full orchestration, 10 composed skills

---

## What this teaches

- Quality of input → quality of output (Test 1 vs Test 4 progression)
- How `meta-paap` handles composition discovery
- How the architecture review step catches over-engineering
- Where `meta-paap` reliably misses (cross-reference [`../05-EVALUATION/regression.md`](../05-EVALUATION/regression.md))

---

## Cross-references

- [`../05-EVALUATION/`](../05-EVALUATION/) — per-test detail and cross-test regression
- [`./README.md`](./README.md) — what `meta-paap` does and when to use it

---

*Populated in Phase 3.*
